package za.co.mawa.bes.controller;

/**
 * Legacy placeholder retained so changed-files deployments can disable the
 * unversioned endpoint without requiring a file deletion. Token refresh is
 * exposed exclusively by controller.v2.RefreshTokenControllerV2.
 */
@Deprecated(forRemoval = true)
public class RefreshTokenController {
}
