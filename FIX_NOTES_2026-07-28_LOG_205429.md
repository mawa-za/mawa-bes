# Log 20260728-205429 verification

The attached backend already includes the application-layer corrections required by the log:

- `SecurityException` is mapped to HTTP 403 instead of HTTP 500.
- `IllegalArgumentException` is mapped to HTTP 400 instead of HTTP 500.
- Published company forms can be listed by ordinary authenticated users.
- Viewing unpublished forms and publishing/unpublishing forms uses protected-administrator capability checks rather than a hard-coded role name.

Regression tests were added for these behaviours. The remaining admin-handoff failure is repaired by the new Flyway tenant migration in the updated `mawa-flyway-runner` archive.
